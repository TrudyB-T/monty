#include "monty.h"

/**
 * f_stack - This sets the format of the data to a stack.
 * @head: the head of the stack.
 * @counter: iterator
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	doc.lifi = 0;
}
